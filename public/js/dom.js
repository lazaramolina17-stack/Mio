export const $ = id => document.getElementById(id);

export const DOM = {
  charForm: $('char-form'),
  charOutput: $('char-output'),
  raceSel: $('char-race'),
  subraceSel: $('char-subrace'),
  subraceGroup: $('subrace-group'),
  classSel: $('char-class'),
  bgSel: $('char-bg'),
  statGrid: $('stat-grid'),
  assignGrid: $('assign-grid'),
  assignSection: $('assign-section'),
  generateBtn: $('generate-btn'),
};